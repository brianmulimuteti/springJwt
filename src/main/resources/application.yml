spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_jwt
    username: root
    password: Pizzarun2018*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect